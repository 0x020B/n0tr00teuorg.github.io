export = deploy;
/**
 * @param {DeployOptions} options
 * @returns {Promise<string>}
 */
declare function deploy({ dir, cid, tag, copyUrl, openUrls, hiddenFiles, uploadServices: uploadServicesIds, pinningServices: pinningServicesIds, dnsProviders: dnsProvidersIds, dnsProvidersCredentials, pinningServicesCredentials, logger }: DeployOptions): Promise<string>;
declare namespace deploy {
    export { DNSLinker, PinningService, PinDirOptions, DeployOptions, Logger };
}
type DeployOptions = import('./types').DeployOptions;
type DNSLinker = import('./dnslinkers/types').DNSLinker;
type PinningService = import('./pinners/types').PinningService;
type PinDirOptions = import('./pinners/types').PinDirOptions;
type Logger = import('./types').Logger;
//# sourceMappingURL=deploy.d.ts.map